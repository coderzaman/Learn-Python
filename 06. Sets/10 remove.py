# remove()
# Description: Removes a specified element from the set. Raises a KeyError if the element is not found.
from audioop import error
s = {1, 2, 3, 4}
s.remove(3)
print(s)

# s.remove(3) // Rais key error